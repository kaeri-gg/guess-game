import { describe, test, it, expect } from 'vitest';
import { setMode } from '../src/js/game';

describe('set mode', () => {
    it('should throw an error if mode is not selected', () => {
        // Arrange
        // Act
        // Assert
    });
});